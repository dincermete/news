; Chatbot tool-call loop can make up to 3 sequential OpenAI HTTP calls
; (see openai.timeout_chatbot / openai.tool_max_rounds). Give it headroom
; above the platform's default 30s so a slow-but-valid response doesn't
; get killed mid-flight and returned to the browser as a broken (non-JSON) page.
max_execution_time = 75
