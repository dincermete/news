/**
 * Realtime (Echo/Reverb). Load only on pages that need live updates:
 * @vite('resources/js/realtime.js')
 * Keep off the default public layout to protect TTFB / page weight.
 */
import './echo';
